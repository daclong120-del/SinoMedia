---
trigger: always_on
---

khi dùng git cấm tự merge, cấm tự commit, cấm tự push

nhưng bạn có thể đề xuất tôi lệnh commit ví dụ 
"git commit -m "docs: update project status, decision log, and agent guidelines for database hardening and guard snapshots"
git push origin main"