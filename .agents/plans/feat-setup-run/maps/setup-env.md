# 🌳 Detail Map — Setup Env & Run

- Setup Env & Run
  - └ Copy files cấu hình `.env` & `.env.local` [quyết định: Tạo bản sao từ file ví dụ] ✅
  - └ Cài đặt package dependencies (`npm install`) [quyết định: npm install] ✅
  - └ Khởi chạy Supabase [quyết định: Bỏ qua Supabase local, chạy Expo Web trước] ✅
  - └ Khởi chạy Web app (`npm run web`) [quyết định: npm run web] ✅
