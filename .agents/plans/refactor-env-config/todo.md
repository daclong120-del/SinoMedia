# ✅ TODO — refactor-env-config

## Phase hiện tại: Phase 2 — Cấu hình supabase edge env local
- [x] Dọn dẹp và tạo `supabase/.env.local` từ `supabase/.env.local.example`
- [x] Thêm các cấu hình `OPENAI_API_KEY` và Cloudflare R2 (`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT_URL`, `R2_BUCKET_NAME`) vào `supabase/.env.local`

## Backlog (chưa xếp phase)
- Không có (Đã hoàn thành toàn bộ các mảng việc chuẩn hóa cấu hình env)
